## production mode
Run `make docker-build` to build app image and start service with **docker compose**

[api](http://localhost:8080/swagger/index.html) 

## dev mode
1. `go mod download`
2. `make` to build and run service **OR** `make local` to run without building

[api](http://localhost:3000/swagger/index.html) 